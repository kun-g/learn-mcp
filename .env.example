# Sitemap MCP Server 配置示例
# 复制此文件为 .env 并修改配置

# 传输模式: stdio (本地模式) 或 http (网络模式)
MCP_TRANSPORT=stdio

# HTTP 模式配置（仅在 MCP_TRANSPORT=http 时生效）
MCP_HOST=127.0.0.1
MCP_PORT=9000

# API Key 认证（仅在 HTTP 模式下生效）
# 留空表示不启用认证（不推荐用于生产环境）
MCP_API_KEY=

# 示例配置:
# HTTP 模式 + 认证
# MCP_TRANSPORT=http
# MCP_HOST=127.0.0.1  
# MCP_PORT=9000
# MCP_API_KEY=your-secret-api-key-here

# HTTP 模式 + 无认证（仅开发测试用）
# MCP_TRANSPORT=http
# MCP_HOST=127.0.0.1
# MCP_PORT=9000
# MCP_API_KEY=